# Ethereum Transaction Simulator Configuration

# Required: Your private key (without 0x prefix)
PRIVATE_KEY=your_private_key_here

# RPC Configuration
RPC_URL=http://127.0.0.1:8545

# Operation Mode: transfer, deploy, or all (default: all)
# - transfer: Send transactions to 25 random addresses
# - deploy: Deploy smart contracts (auto-generated)
# - all: Run transfers and contract deployment/interaction in parallel
MODE=all

# Transaction Settings
VALUE=1
TX_DATA=lets bomb the network with transactions! AMF to the moon : ) 🚀
MAX_TRANSACTIONS=10000
DELAY_SECONDS=0
GAS_LIMIT=210000
RETRY_DELAY=10

# Parallel Mode Settings (only for MODE=parallel)
MIN_BALANCE=100000
WALLET_COUNT=1000
FUNDING_AMOUNT=100
